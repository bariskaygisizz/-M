{
  "name": "balikatlas",
  "version": "1.0.0",
  "private": true,
  "description": "BalıkAtlas — Türkiye balık rehberi (App Store uyumlu Expo uygulaması)",
  "scripts": {
    "mobile": "npm run start --prefix mobile",
    "install:all": "npm install --prefix mobile"
  }
}
