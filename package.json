{
  "name": "balikatlas",
  "version": "2.0.0",
  "private": true,
  "description": "BalıkAtlas — AI kamera ile balık tanıma, frontend + backend",
  "scripts": {
    "server": "npm run start --prefix server",
    "web": "npm run dev --prefix web",
    "mobile": "npm run start --prefix mobile",
    "install:all": "npm install --prefix server && npm install --prefix web && npm install --prefix mobile"
  }
}
